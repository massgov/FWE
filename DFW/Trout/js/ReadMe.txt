NOTES on Wildlands JS and the JSON

The JSON can contain either a site name(s) or a x,y, and zoom level. The page accepts SITE_NAME or x,y, and zoom level as parameters

The legend is a gif that is not connnected to the layers.

